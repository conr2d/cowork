# WP6 — Full gate runbook (v0.2 definition of done)

The **authoritative** v0.2 acceptance gate. Like the v0.1 gate it runs **manually /
semi-automated** on a real Windows clean-room; everything cheaply automatable already runs
per-push in `ci.yml` (frontend, guest/host unit tests, conformance, docker `guest-e2e`,
real-WSL2 `wsl-integration`, and the windows-latest `host` job that compiles `src-tauri`).
This gate covers what those cannot: the real ConPTY multi-session lifecycle, real agent
resume across app restarts, real first-run sign-in flows, and the v0.1 → v0.2 **upgrade**
path.

**Clean-room mechanics (VM creation, checkpoints, baseline verification, i18n passes) are
identical to v0.1 — follow [`wp10-full-gate.md`](./wp10-full-gate.md) §1–§3 and reuse
`scripts/wp10/fullgate.ps1`.** This document only defines what v0.2 adds or changes.

Scope sealed on `main` as of `0f03e5f`: WP0–WP5 + boot-time guest re-injection
(multi-session PTY `7d08d9b`, session tabs + claude resume `9184f48`, codex/agy
continuity `479ee4d`, presets `37b1bf8`, upgrade healing `2f9a6d1`), plus four changes made
after that scope was first written:

- `816317a` — **no eager auth.** Cowork never runs an agent's login command; each agent
  drives its own sign-in on first launch. (§4, §8)
- `1e9fe85` — toolchain activation moved to `~/.profile`, so every *login* shell has brew
  and mise on `PATH`. Verified on the real WSL2 kernel by CI's `wsl-integration` job; **not
  a gate item.**
- `eab7522` — **credentials are no longer routed.** Each agent uses its own default config
  directory; nothing is created under `~/.cowork`. (§1, §7, §8)
- `0f03e5f` — **`agent-install` resolves before installing.** An agent already on a login
  shell's `PATH` is used as-is, never reinstalled. (§4)

---

## 1. Prerequisites (delta over wp10 §1)

| # | Item | Check |
|---|------|-------|
| 1 | **Fresh v0.2 `Cowork.exe` built from current `main`** | artifact `headSha` == `git rev-parse main`. ⚠️ A stale installer embeds a stale guest — it would mask the re-injection scenario AND break protocol v4 features. |
| 2 | **A v0.1-completed checkpoint** for the upgrade path | a VM checkpoint taken *after* a passing v0.1 happy path (wizard finished, marker written, default workspace exists). Name it `v01-setup-complete`. If none survives, run wp10 §4 once with the v0.1 installer first. |
| 3 | Agent accounts (claude + codex + antigravity) | tester can sign in AND out (codex: `codex logout`, claude: `claude auth logout` inside the distro) — §7 first-run coverage needs a signed-out state. Credentials now live in each agent's own directory (`~/.claude`, `~/.codex`, `~/.gemini/antigravity-cli`), so that is where a sign-out must take effect. |
| 4 | `ready-to-install` clean checkpoint (wp10 §3) | for the fresh path. |

---

## 2. Entry paths

Run **both**. Path A is the new release-blocking coverage; Path B re-validates the wizard
with the v0.2 deltas.

- **Path A — upgrade healing:** revert to `v01-setup-complete` → install/replace with the
  v0.2 `Cowork.exe` → launch. Continue at §3.
- **Path B — fresh install:** revert to `ready-to-install` → install v0.2 → launch.
  Continue at §4 (wizard), then everything from §5 onward.

---

## 3. Path A — upgrade healing (boot-time guest re-injection)

| Step | Action | Expected (PASS) |
|------|--------|-----------------|
| 3.1 | Before first launch, fingerprint the stale guest: `wsl -d Cowork -- sha256sum /usr/local/bin/cowork` | hash recorded (this is the v0.1 guest) |
| 3.2 | Launch the v0.2 app | boot gate skips the wizard (marker honored) and lands in the **shell** (sidebar + default workspace), not the wizard |
| 3.3 | Re-run the sha256 from 3.1 after the shell has loaded | **hash changed** — `guest_sync` re-injected; binary is executable (`wsl -d Cowork -- /usr/local/bin/cowork --help` lists `session-uuid`, `agent-theme`) |
| 3.4 | Create a workspace with the **PDF translate** preset | succeeds (proves the new guest's `--preset` arg end-to-end); `AGENTS.md` + `CLAUDE.md` appear per §5.3 |
| 3.5 | Second app launch | boot is NOT slower than the first (sync compares bytes and skips injection — no repeated copy) |
| 3.6 | v0.1 data intact | the v0.1 `default` workspace and its `files/` content survived; metadata (`%LOCALAPPDATA%\Cowork\workspaces.json`) still lists it |

---

## 4. Path B — wizard deltas (no sign-in step)

Run wp10 §4 (happy path) with the v0.2 installer; steps 4.1–4.7 are unchanged. Replace the
old 4.8–4.9 expectations with:

| Step | Action | Expected (PASS) |
|------|--------|-----------------|
| 4.1 | After agent install | the wizard shows the **done step** — no terminal, no login buttons, no auto-advance. Clicking "Open Cowork" enters the shell |
| 4.2 | Relaunch after clicking **Open Cowork** | marker written; relaunch boots straight to the shell |
| 4.3 | After finish: `wsl -d Cowork -- bash -lc 'ls -d ~/.cowork'` | **no such directory** — credentials are not routed |
| 4.4 | Re-run the install by hand: `wsl -d Cowork -u cowork -- /usr/local/bin/cowork agent-install --agent claude` | returns in **seconds** with no installer output — the agent already on `PATH` is resolved, not reinstalled. `claude --version` still passes |
| 4.5 | Pre-seeded binary: from a `ready-to-install` run, stop after the distro exists, install an agent by hand into a non-default location on `PATH` (e.g. `sudo install -m0755 ~/.local/bin/claude /usr/local/bin/claude && rm ~/.local/bin/claude`), then run `cowork agent-install --agent claude` | resolves `/usr/local/bin/claude` and skips the install; **no** `~/.local/bin/claude` reappears |

---

## 5. Shell core — workspaces + presets

| Step | Action | Expected (PASS) |
|------|--------|-----------------|
| 5.1 | Create workspaces from the sidebar ⊕ (one per preset: blank / PDF translate / proposal; give one a Korean display name) | each appears in Recent, slug ASCII-derived, terminal opens at `~/workspaces/<slug>` |
| 5.2 | Blank preset | NO `AGENTS.md`, NO `CLAUDE.md` at the workspace root |
| 5.3 | PDF / proposal presets | `AGENTS.md` present with the matching template; `CLAUDE.md` is a **symlink → AGENTS.md** (`wsl -d Cowork -- ls -l ~/workspaces/<slug>`); spawning **claude** in that workspace shows the instructions are picked up (e.g. ask "what are this workspace's instructions?") — repeat once with **codex** (reads AGENTS.md directly) |
| 5.4 | Explorer button (WP3 regression) | opens `files\` of the active workspace in Windows Explorer; a file dropped there is visible from the agent |
| 5.5 | Pin/unpin + drag-reorder pinned (WP2b regression) | order persists across restarts |
| 5.6 | Delete a workspace whose session is running | its terminal disappears, its PTY dies (no orphan `wsl.exe` child for it), metadata row gone |

---

## 6. Sessions — multi-PTY, tabs, glyphs

| Step | Action | Expected (PASS) |
|------|--------|-----------------|
| 6.1 | Visit workspace A (claude autoruns), start a long agent task; switch to workspace B; wait; switch back | A's session **kept running** (output progressed while hidden); no respawn, scrollback intact; TUI not corrupted after the switch (no 0×0 resize artifacts) |
| 6.2 | Tab strip: ⊕ | new tab with the workspace default agent, title `Claude 2`-style, becomes active |
| 6.3 | ▾ picker → another agent | session spawns with that agent AND the workspace default agent updates (next ⊕ uses it) |
| 6.4 | Status glyphs | cold (restored, not yet activated) → idle after spawn → **working pulse** while the agent streams → idle ~2 s after output stops → **done** when the process exits (type `exit` in a shell-ish session or `/quit` the agent) |
| 6.5 | Second concurrent session in the same workspace | one-shot advisory bar appears (shared-files warning); dismissible; does not reappear for the third |
| 6.6 | Close a tab (×) | PTY killed, metadata updated; closing the **last** tab immediately opens a fresh default-agent tab |
| 6.7 | Close the app window with 2+ live sessions across workspaces | all `wsl.exe` ConPTY children die (check Task Manager / `Get-Process wsl` before/after) — the `WindowEvent::Destroyed → kill_all` backstop |

---

## 7. Session continuity (the v0.2 core promise)

For each agent — claude, codex, antigravity — in its own workspace:

| Step | Action | Expected (PASS) |
|------|--------|-----------------|
| 7.1 | New session, send a first message ("remember the word *kumquat-7*"), wait for the reply and ~5 s of quiet | for codex/agy: `workspaces.json` gains the session's `agentSessionUuid` (lazy capture after idle); claude's was set at creation |
| 7.2 | Quit and relaunch the app | tab strip rebuilt from metadata; tabs show **cold** glyphs; nothing spawns until activated (lazy restore) |
| 7.3 | Activate the tab | spawn uses the resume form (`claude --resume`, `codex resume`, `agy --conversation`); asking "what word did I ask you to remember?" returns *kumquat-7* |
| 7.4 | claude edge: create a session, type nothing, restart, activate | claude prints its own "no conversation found"-style error in the terminal (accepted); opening a new tab works |
| 7.5 | Two codex sessions in the SAME workspace, messages in both | each tab resumes its own conversation after restart (cwd+time-keyed capture attributed correctly) |
| 7.6 | `wsl -d Cowork -- bash -lc 'ls ~/.codex/sessions ~/.gemini/antigravity-cli/conversations'` | both exist and hold the sessions from 7.1. The uuid capture reads the agent's **own** directory; anything under `~/.cowork` would mean the capture root regressed |

---

## 8. First-run sign-in (agent self-driven)

| Step | Action | Expected (PASS) |
|------|--------|-----------------|
| 8.1 | Signed out of claude, open a claude session | the terminal runs bare `claude …`; the agent's own onboarding + login runs **once**; after signing in, the same terminal drops into the agent. Reopening a session never prompts again |
| 8.2 | Same for codex and antigravity | codex shows its `Sign in with ChatGPT` screen; antigravity drives its own login |
| 8.3 | The agent prints an OAuth URL and the host browser does not open | the **"Open sign-in page"** button appears above the session terminal and opens it. Release-blocking: this is the only escape hatch |
| 8.4 | After each sign-in, inspect the distro | credential material lands in the agent's own directory (`~/.claude`, `~/.codex`, `~/.gemini/antigravity-cli`) and `~/.cowork` still does not exist |

---

## 9. Theming

| Step | Action | Expected (PASS) |
|------|--------|-----------------|
| 9.1 | Light theme + new **antigravity** session | before spawn, `~/.gemini/antigravity-cli/settings.json` has `"colorScheme": "solarized light"`; agy output legible on light |
| 9.2 | Dark theme + new agy session | `"tokyo night"`; legible |
| 9.3 | Light theme across all three agents (spawned ON light) | prompts, spinners, markdown output legible (codex detects via OSC 11 at spawn) |
| 9.4 | Mid-session theme flip with a live codex session | **known limitation**: stale background bands may appear; a NEW codex session renders correctly. No fix expected in v0.2 — just confirm no crash |

---

## 10. Failure induction (delta)

| Case | Induce | Expected (PASS) |
|------|--------|-----------------|
| 10.1 | Corrupt the installed guest: `wsl -d Cowork -u root -- bash -c 'echo broken > /usr/local/bin/cowork'`, relaunch the app | shell boots; guest re-injected (sha matches shipped); workspace create works |
| 10.2 | Break the probe path only (e.g. `chmod -x` the guest), launch, then watch first session mount | boot proceeds (sync/probe failures are best-effort); after the heal in 10.1's mechanism or a relaunch, features recover; errors that do surface are localized envelopes, not raw panics |
| 10.3 | Kill `wsl.exe` for a running session from Task Manager | tab flips to **done**; app stays responsive; new sessions spawn fine |

---

## 11. Recording

Append results to this file's PASS/FAIL log (date, installer sha, path A/B, locale, per-§
verdicts, deviations). A FAIL on §3 (upgrade), §6.7 (kill_all), §7 (continuity), or §8
(first-run sign-in) blocks the v0.2 release; cosmetic deviations get filed for
v0.2-polish triage.

## Log

| Date | Installer sha | Path | Locale | Verdict | Notes |
|------|---------------|------|--------|---------|-------|
